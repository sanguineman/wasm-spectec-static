(library
 (name p4)
 (libraries util concrete domain xl il core_unix menhirLib)
 (modules
  source
  context
  extract
  parser
  error
  lexer
  preprocessor
  parse
  parser_debug
  debug_config))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain --table --inspection))
