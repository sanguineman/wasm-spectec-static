syntax env

syntax value =
  | NumV int          ;; n
  | CloV id expr env  ;; <λx.e, env>
  | LocV nat          ;; loc

syntax env = map<id, value>
dec $lookup_env(env, id): value?

syntax sto = value*
