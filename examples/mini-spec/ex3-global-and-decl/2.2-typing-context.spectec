syntax pair<K, V> = K -> V
syntax map<K, V> = (pair<K, V>)*

syntax contextLayer = map<id, type>
dec $lookup_context(contextLayer, id) : type?

syntax context =
  { GLOBAL contextLayer,
    LOCAL contextLayer }
var C : context
